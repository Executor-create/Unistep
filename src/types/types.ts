export type FormValues = {
  email: string;
  password: string;
  username: string;
  number: string;
  birthday: string;
  avatar: string;
};

export type User = {
  username: string;
  email: string;
  password: string;
};
